# Core Data Science & ML
pandas>=2.1.4
numpy>=1.26.0
scikit-learn>=1.3.2
xgboost>=2.0.3

# Deep Learning (TF 2.16+ is required for Python 3.12)
tensorflow>=2.16.1

# Explainability
shap>=0.44.0
lime>=0.2.0.1

# API & Web
flask>=2.3.2
flask-cors>=4.0.0
streamlit>=1.31.0
groq>=0.4.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0
nbformat>=5.10.0
Pillow>=10.2.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0.1
requests>=2.31.0
python-dateutil>=2.8.2
tqdm>=4.66.0

# Monitoring
scipy>=1.11.0

# Production (optional)
gunicorn>=21.2.0; sys_platform != 'win32'
waitress>=2.1.2

# --- Optional extras (install only if needed) -------------------------------
# Geospatial (notebooks 08/maps) — Windows wheels exist but install is heavier:
#   pip install geopandas>=1.0.0 folium>=0.15.0
#
# Generative AI (notebook 05) — pulls PyTorch:
#   pip install ctgan>=0.10.2
#
# CNN data augmentation (notebook 06):
#   pip install albumentations>=1.4.0
#
# Postgres audit log (docker-compose only):
#   pip install sqlalchemy>=2.0.20 psycopg2-binary>=2.9.9
#
# Prometheus metrics export:
#   pip install prometheus-client>=0.19.0
#
# Dev tooling:
#   pip install pytest>=7.4 pytest-cov>=4.1 black>=23.9 flake8>=6.1 isort>=5.12
